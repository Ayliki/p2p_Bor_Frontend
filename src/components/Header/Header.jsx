import cl from './styles.module.css'

const Header = () => {
    return(
        <header className={cl.header}>
            <h1>Safely Exchange Money</h1>
        </header>
    )
}

export default Header